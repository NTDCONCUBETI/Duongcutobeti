local _autoFarmActive = false
local _autoSellActive = false
local player = game.Players.LocalPlayer
local targetPath = workspace:WaitForChild("Plots"):WaitForChild("3"):WaitForChild("3"):WaitForChild("PlotSurface"):WaitForChild("Part")

-- Khai báo Service và Remotes
local Rep = game:GetService("ReplicatedStorage")
local KnitPath = Rep:WaitForChild("Packages"):WaitForChild("_Index"):WaitForChild("sleitnick_knit@1.7.0"):WaitForChild("knit"):WaitForChild("Services")

local StartRunRF = KnitPath:WaitForChild("RunningService"):WaitForChild("RF"):WaitForChild("StartRun")
local ReloadCharRF = KnitPath:WaitForChild("PlayerService"):WaitForChild("RF"):WaitForChild("ReloadCharacter")
local OpenLuckyBlockRF = KnitPath:WaitForChild("RunningService"):WaitForChild("RF"):WaitForChild("OpenLuckyBlock")
local SellRemote = KnitPath:WaitForChild("InventoryService"):WaitForChild("RF"):WaitForChild("SellBrainrot")

-- Khởi tạo ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MonkeyAutoFarm_V3"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- Khung chứa Main (Draggable)
local MainFrame = Instance.new("Frame")
MainFrame.Parent = ScreenGui
MainFrame.Size = UDim2.new(0, 160, 0, 120)
MainFrame.Position = UDim2.new(0.5, -80, 0.5, -60)
MainFrame.BackgroundTransparency = 1
MainFrame.Active = true
MainFrame.Draggable = true

-- Nút Auto Farm
local FarmButton = Instance.new("TextButton")
FarmButton.Parent = MainFrame
FarmButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
FarmButton.Size = UDim2.new(1, 0, 0, 50)
FarmButton.Font = Enum.Font.SourceSansBold
FarmButton.Text = "Off_AutoFarm"
FarmButton.TextColor3 = Color3.new(1, 1, 1)
FarmButton.TextSize = 18
Instance.new("UICorner", FarmButton).CornerRadius = UDim.new(0, 8)

-- Nút Auto Sell
local SellButton = Instance.new("TextButton")
SellButton.Parent = MainFrame
SellButton.BackgroundColor3 = Color3.fromRGB(170, 0, 0)
SellButton.Position = UDim2.new(0, 0, 0, 60)
SellButton.Size = UDim2.new(1, 0, 0, 50)
SellButton.Font = Enum.Font.SourceSansBold
SellButton.Text = "Off_AutoSell"
SellButton.TextColor3 = Color3.new(1, 1, 1)
SellButton.TextSize = 18
Instance.new("UICorner", SellButton).CornerRadius = UDim.new(0, 8)

-- Nút X (Close)
local CloseButton = Instance.new("TextButton")
CloseButton.Parent = MainFrame
CloseButton.Size = UDim2.new(0, 20, 0, 20)
CloseButton.Position = UDim2.new(1, -22, 0, 2)
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.new(1, 1, 1)
CloseButton.ZIndex = 10
Instance.new("UICorner", CloseButton).CornerRadius = UDim.new(0, 4)

-- Nút Thu nhỏ (-)
local MinButton = Instance.new("TextButton")
MinButton.Parent = MainFrame
MinButton.Size = UDim2.new(0, 20, 0, 20)
MinButton.Position = UDim2.new(1, -44, 0, 2)
MinButton.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
MinButton.Text = "-"
MinButton.TextColor3 = Color3.new(1, 1, 1)
MinButton.ZIndex = 10
Instance.new("UICorner", MinButton).CornerRadius = UDim.new(0, 4)

-- Nút Con Khỉ (Khi thu nhỏ)
local MonkeyIcon = Instance.new("ImageButton")
MonkeyIcon.Parent = ScreenGui
MonkeyIcon.Size = UDim2.new(0, 60, 0, 60)
MonkeyIcon.Position = UDim2.new(1, -70, 1, -70)
MonkeyIcon.Image = "rbxassetid://13112423377"
MonkeyIcon.Visible = false
MonkeyIcon.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Instance.new("UICorner", MonkeyIcon).CornerRadius = UDim.new(1, 0)

---------------- LOGIC AUTO SELL (BẢN FIX CHUẨN) ----------------
local function runAutoSell()
    while _autoSellActive do
        local success, err = pcall(function()
            local items = {}
            -- Quét túi và nhân vật
            for _, v in ipairs(player.Backpack:GetChildren()) do if v:IsA("Tool") then table.insert(items, v) end end
            if player.Character then
                for _, v in ipairs(player.Character:GetChildren()) do if v:IsA("Tool") then table.insert(items, v) end end
            end

            for _, tool in ipairs(items) do
                if not _autoSellActive then break end
                
                local entityId = tool:GetAttribute("EntityId")
                if entityId then
                    -- Lấy mutation, nếu nil thì mặc định là "NORMAL" để tránh lỗi index nil
                    local mutAttr = tool:GetAttribute("Mutation")
                    local mutation = tostring(mutAttr or "NORMAL"):upper()
                    
                    if mutation ~= "VOID" then
                        SellRemote:InvokeServer(entityId)
                        task.wait(0.05)
                    end
                end
            end
        end)
        
        if not success then warn("Auto Sell Error: " .. tostring(err)) end
        task.wait(2) -- Quét lại mỗi 2 giây cho ổn định
    end
end

---------------- LOGIC AUTO FARM ----------------
local function startFarmLoop()
    while _autoFarmActive do
        task.wait()
        task.spawn(function() pcall(function() OpenLuckyBlockRF:InvokeServer("base14") end) end)
        pcall(function()
            if player.Character and targetPath then
                player.Character:PivotTo(targetPath.CFrame + Vector3.new(0, 10, 0))
            end
        end)
    end
end

---------------- SỰ KIỆN CLICK ----------------
FarmButton.MouseButton1Click:Connect(function()
    _autoFarmActive = not _autoFarmActive
    FarmButton.Text = _autoFarmActive and "On_AutoFarm" or "Off_AutoFarm"
    FarmButton.BackgroundColor3 = _autoFarmActive and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
    if _autoFarmActive then
        task.spawn(function()
            pcall(function() StartRunRF:InvokeServer() end)
            pcall(function() ReloadCharRF:InvokeServer() end)
        end)
        task.spawn(startFarmLoop)
    end
end)

SellButton.MouseButton1Click:Connect(function()
    _autoSellActive = not _autoSellActive
    SellButton.Text = _autoSellActive and "On_AutoSell" or "Off_AutoSell"
    SellButton.BackgroundColor3 = _autoSellActive and Color3.fromRGB(0, 170, 0) or Color3.fromRGB(170, 0, 0)
    if _autoSellActive then task.spawn(runAutoSell) end
end)

MinButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
    MonkeyIcon.Visible = true
end)

MonkeyIcon.MouseButton1Click:Connect(function()
    MainFrame.Visible = true
    MonkeyIcon.Visible = false
end)

CloseButton.MouseButton1Click:Connect(function()
    _autoFarmActive = false
    _autoSellActive = false
    ScreenGui:Destroy()
end)

---------------- ANTI-AFK XỊN ----------------

task.spawn(function()
    local vim = game:GetService("VirtualInputManager")
    while true do
        task.wait(2) -- 2 phút  check một lần cho chắc
        -- Giả lập nhấn phím nhảy (Space)
        vim:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
        task.wait(0.1)
        vim:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
        print("Anti-AFK: Đã thực hiện nhảy để giữ kết nối")
    end
end)

print("Script Load Done!")
